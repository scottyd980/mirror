<div class="container">
  <div class="row">
    <div class="col-md-12">
      {{#x-card title="History"}}
        {{#if model.retrospectives}}
          {{x-list/retrospective
            retrospectives=model.retrospectives
            isAdmin=isAdmin
            onDelete=(action "deleteAction")
          }}
        {{else}}
          It doesn't look like this team has had any retrospectives yet.
        {{/if}}
      {{/x-card}}
    </div>
  </div>
</div>
