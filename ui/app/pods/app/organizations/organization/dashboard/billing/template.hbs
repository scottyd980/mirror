<div class="container">
  <div class="row">
    <div class="col-md-8">
      {{#x-card title="Cards On File"}}
        {{#each model.cards as |card|}}
          <strong>{{card.brand}} xxxxx-{{card.last4}}</strong>
          Expires {{card.exp_month}}/{{card.exp_year}} • Added {{moment-format card.added_date "MMMM Do, YYYY"}}
          <br/>
        {{/each}}
        There are currently no cards stored for your organization.
      {{/x-card}}
    </div>
    <div class="col-md-4">
        {{x-billing-card 
          title="Add New Billing Information"
          afterSubmission=(route-action "newOrganizationBilling")
        }}
    </div>
  </div>
</div>