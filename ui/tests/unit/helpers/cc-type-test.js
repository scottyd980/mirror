
import { ccType } from 'mirror/helpers/cc-type';
import { module, test } from 'qunit';

module('Unit | Helper | cc type');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = ccType([42]);
  assert.ok(result);
});

